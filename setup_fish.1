curl -sL https://git.io/fisher | source && fisher install jorgebucaran/fisher &&
fisher install IlanCosman/tide@v5 &&
cp -r ./.config/fish $HOME/.config/
fisher install gazorby/fish-exa
set -Uxa EXA_STANDARD_OPTIONS --icons
